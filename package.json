{
  "name": "bitcoinvn-otc-landing",
  "version": "1.0.0",
  "description": "BitcoinVN OTC Trading Service Landing Page",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "NODE_ENV=production npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
